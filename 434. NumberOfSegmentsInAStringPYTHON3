class Solution:
    def countSegments(self, s: str) -> int:
        return len(s.split())

**************************************************************

class Solution:
    def countSegments(self, s: str) -> int:
        count = 0
        i = len(s)-1

        while i>=0:
            if s[i] != " ":
                count += 1
                while i>=0 and s[i] != " ":
                    i-=1
            i-=1
        return count
