class Solution:
    def removeStars(self, s: str) -> str:
        stack = []
        result = ""

        i = 0

        while i < len(s):
            if s[i].isalpha():
                stack.append(s[i])
            else:
                stack.pop()

            i += 1
        
        result = "" .join(stack)
        return result
